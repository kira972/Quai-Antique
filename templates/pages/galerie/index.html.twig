{% extends 'base.html.twig' %}

{% block title %}Quai Antique - Galerie
{% endblock %}

{% block body %}
	<section id="food-menu">
		<div class="container">
			<header class="section-header">
				<h1 class="mb-5">~ Galerie photos ~</h1>
			</header>
			<div class="row mt-4">
{% for pictureInGallery in picturesInGallery %}

				<div class="col-sm-6 col-md-4 col-lg-3">
					<div class="single-menu">
<img class="img-fluid" src="{{asset("build/images/" ~ pictureInGallery.filename)}}"/>

					</div>
				</div>
{% endfor %}
			</div>
			<div>
				<a id="btn-rounded" class="mt-6" href="{{ path("app_reservation") }}">Réserver</a>
			</div>
		</section>
		{{ include("_partials/_contact.html.twig", {'openingTimes': openingTimes, 'restaurant': restaurant}) }}
	{% endblock %}
